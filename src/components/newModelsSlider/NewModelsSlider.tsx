import styles from './NewModelsSlider.module.scss';

export const NewModelsSlider = () => {
  return (
    <section className={`${styles.section} ${styles.newModels}`}>
      <h3 className={styles.sectionTitle}>Brand new models</h3>

      <div>card</div>
    </section>
  );
};
